# Valve game content — do not publish (copyright + size)
valve.zip
hlds/

# Generated by ./prepare-client.sh from the pinned Docker image
web/main.js

# Note: custom-maps/*.bsp are bundled in the repo (committed) so custom maps
# ship with it. ./add-maps.sh re-injects them into a freshly built valve.zip.

node_modules/
screenshot.png
package-lock.json
